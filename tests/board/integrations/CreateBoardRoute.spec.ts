describe('Create Board Route', () => {

    beforeEach(() => {
    });

    it('should be able to create a board', async () => {
    });

    it('should be not able to create a board with non-existing name', async () => {
    });

});